# 执行进度审核报告（独立复核）

> **HISTORICAL_SNAPSHOT / SUPERSEDED。** 本文保留 2026-08-06 的审核快照，不代表当前阶段；现役状态见 [PROJECT_STATUS.md](./PROJECT_STATUS.md)。

版本：v1.0
日期：2026-08-06（晚间，F0-I 收口后）
审核方式：独立只读复核（直接检查产物 SHA、迁移链、测试文件、容器与运行环境，非仅采信文档）
状态：`F0-A~F0-I 全部已交付 / RAG 未开工 / NOT_PRODUCTION`

> 本报告第 2 节取代《后续开发路线_成熟方案优先_2026-08-06.md》第 1 节"当前执行快照"（该快照写于 F0-I 收口前，已过时）；该文档第 2～6 节（自建/采用边界、准入门、实施顺序、候选池、完成定义）仍然有效。

---

## 1. 审核结论（TL;DR）

1. **F0-I 已完成收口**，不再是"进行中"。三项产物已真实存在且 SHA 与 PROGRESS.md 登记值逐字一致；全仓 690 项测试 OK；reverse 严格 13 行全绿。
2. **F0-A～F0-I 九个阶段全部交付**，证据链完整；当前诚实状态仍是 `LOCAL_CANONICAL_CHUNKS_READY / ACCURACY_NOT_EVALUATED / SEARCH_NOT_READY / NOT_PRODUCTION`。
3. **RAG、embedding、检索、引用、问答均未开工**，正好处于"成熟方案优先"路线的切入点。
4. **新查明一个选型硬风险**：RAGFlow 官方不提供 arm64 Docker 镜像（本机是 Apple M5 Max，arm64），必须自行构建；OpenSearch 官方镜像支持 arm64。这直接影响 F0-J0 选型顺序，已写入任务书。
5. 下一步唯一可执行任务是 **F0-J0（成熟检索方案选型与可撤销探针）**，任务书已单独产出：`F0-J0_TASKBOOK.md`，可直接交给 dsv4 flash 执行。

---

## 2. 本次独立复核证据（2026-08-06 实测）

| 检查项 | 实测结果 | 判定 |
|---|---|---|
| `artifacts/f0i-canonical-chunks/v0.1/` | 3 文件存在（acceptance.json / status.html / sbom.json），权限 0600 | ✅ |
| 三产物 SHA-256 实算 | acceptance=`8a4c58cf…897d1a`、status=`eb38e014…f9cc5a`、sbom=`b7fa245e…c4e8b5`，与 PROGRESS.md 登记值逐字一致 | ✅ |
| acceptance.json 内容 | full=26 文档/249 视觉单元/225 native/24 OCR/1909 blocks/253 parent/300 child/1636 links；二次 full 零新增零 OCR；11 个业务闸门全 CLOSED | ✅ |
| Alembic 迁移链 | `f0d_0001→0002→0003→0004→0005→0006` 严格线性、唯一 head | ✅ |
| 测试方法计数 | 9 个测试文件实数 690 个 `def test_`，与"Ran 690 tests / OK"登记一致 | ✅ |
| PostgreSQL 容器 | `anhuan-f0d-postgres-1` running healthy | ✅ |
| 密钥/锁文件 | `/private/tmp/anhuan-f0i-acceptance-v01.key`、`anhuan-f0i-replay.lock` 存在 | ✅（但见风险 R2） |
| SBOM 组件 | CPython 3.11.9 / PostgreSQL 18.3 / pgcrypto 1.4 / pypdf 6.14.2 / RapidOCR 3.9.2 / ONNX Runtime 1.28.0 / PP-OCRv6-small det+rec，全部锁 hash | ✅ |
| 本机资源 | Apple M5 Max / 18 核 / 128GB 内存 / 磁盘余 866GB / macOS 26.4 | ✅ 资源不是瓶颈 |
| 版本控制 | 项目目录**无 .git** | ⚠️ 见风险 R1 |

## 3. 阶段状态矩阵

| 阶段 | 交付物 | 最终状态 | 证明了什么 / 没证明什么 |
|---|---|---|---|
| F0-A | 26 份原件身份闸门 | 已交付 | 原件 SHA/身份可验证；未证明内容质量 |
| F0-B | 5 类格式安全路由 | 已交付 | 格式识别失败关闭；未证明解析正确 |
| F0-C | 页级原生解析/OCR 分流计划 | 已交付 | 249 视觉单元确定性计划；未执行 OCR |
| F0-D | PostgreSQL RLS + 上传底座 + 五 P0 关闭闸门 | 已交付 | 租户隔离/幂等/恢复可重放；非生产 |
| F0-E | 本地 OCR 执行证据（断网容器） | 已交付 | OCR 可执行且正文零落盘；未评准确率 |
| F0-F | pgcrypto 受控正文 + 15 页双标队列 | 已交付 | 正文只存密文；人工标注未执行 |
| F0-G | 盲审标注工作流（loopback API） | 已交付 | 三角色盲审底座就绪；真实 label=0 |
| F0-H | PP-OCRv6 离线运行时（可回滚 v4） | 已交付 | v6 可复现执行；`ACCURACY_NOT_EVALUATED` |
| F0-I | canonical blocks / parent-child chunks | **已交付（本日收口）** | 加密可重组可追溯切块；`SEARCH_NOT_READY` |

**尚未开始**：embedding、混合检索、重排、引用回答、问答 LLM、OIDC、对象存储、前端、企业档案/整改/日历/报告等全部 F0-J 之后的内容。

**全局门禁不变**：真实客户、地区行业、Acceptance Gold、外部 OCR/LLM、专业责任、UAT、生产均 CLOSED；探针类工作不得开闸。

## 4. 关键新情报：RAGFlow 的 arm64 硬门槛

本机为 Apple M5 Max（arm64）。经查证官方口径：

- RAGFlow **官方只维护 x86 Docker 镜像，不提供 arm64 镜像**；arm64 用户须按官方文档自行构建（darwin/arm64 可构建），且需上调 xgboost 版本、正确安装 unixODBC；Infinity 文档引擎在 arm64 上官方不支持。
- 社区在 Apple Silicon 上长期报告平台不匹配问题（linux/amd64 镜像在 arm64 宿主报警/报错），第三方评估认为社区补丁"可用但不稳定"。
- OpenSearch 官方镜像原生支持 arm64，无此问题。

结论：**RAGFlow 在本机的准入成本显著上升**，选型探针必须把"arm64 可行性"设为最早的硬门（构建失败即按准入门规则留证据后转 OpenSearch 单路线）。这一裁决已固化进 F0-J0 任务书，避免弱模型在构建泥潭里空转。

参考：RAGFlow FAQ（官方仅支持 x86）、RAGFlow 构建文档（arm64 需自建）、GitHub issue #253/#1164（Apple Silicon 平台不匹配）。

## 5. 风险与优化建议

按影响排序：

- **R1（最高）项目无版本控制**：全部代码、迁移、测试、任务书都只存在于本机单目录，无 .git。任何误删/磁盘故障将丢失全部工程（原件另有登记根，但代码不可再生）。**建议：立即 `git init` 并做首次提交**；`.gitignore` 排除 `.venv/`、`__pycache__/`、`fixtures/`（真实资料不进 git）、`.render-*`；`artifacts/`、`infra/`（含锁文件与模型元数据）应提交。此事 5 分钟可完成，建议在交给 dsv4 flash 之前由你确认执行。
- **R2 密钥放在 /private/tmp**：F0-F/F0-I 的 pgcrypto keyfile 在 `/private/tmp`，macOS 重启或周期清理可能删除；密钥丢失后所有密文正文不可解，需要整轮重放（重放是确定性的、可恢复，但证据连续性中断）。**建议**：短期在 F0-J0 任务书 Task 0 加了"密钥存在性检查，缺失即 BLOCKED"；中期（F1 前）单独立项把 vault/key 迁到持久化 0700 目录并重登记。
- **R3 稳定性证明边界**：现有 690 项测试只证明"26 份 Fixture 在本机可重复"，不证明真实客户文件分布、并发、异常重启下的稳定性。这已在各产物中诚实标注；真正的补强依赖 Q04（真实文件分布）确认后的扩样，不是现在多写测试能解决的。维持现状即可，不要提前优化。
- **R4 聊天泄漏事故模式**：BLOCKED.md 记录了多次同型事故（错误工作目录跑 `shasum -c`、批量 `sed/find`、AST 反解把 DSN/文件名带进聊天）。dsv4 flash 更弱，更容易复发。**已在任务书中固化防护**：只允许白名单诊断命令、一切输出只准聚合计数/布尔/退出码、禁止打印 DSN/文件名/chunk 正文、禁止对 config/bootstrap 文件做整文件输出。
- **R5 单租户 Fixture 限制**：26 份资料同属一个企业，"零跨租户候选"无法用真实数据验证。任务书采用合成租户 B（只进索引、不进 PostgreSQL），并利用"合成 chunk 能命中索引但必然过不了 PG 再鉴权"这一点，反向证明"引擎结果 ≠ 授权边界"。
- **R6 历史残留待授权清理**：13 个 `f0f_test_*` 孤儿数据库、14 个 `anhuan-f0f-dbtest-*.key`、多个 `anhuan-f0h-*` 构建临时目录。均已登记、不阻塞。建议你单独授权一次只读盘点+定向清理任务（不要混进 F0-J0）。
- **R7 规划文档双事实源**：《后续开发路线》§1 快照已过时。本报告显式取代该节；F0-J0 收口时应由执行者把新状态写回 PROGRESS.md（任务书已要求），路线文档本身保持不动，避免第三套状态。

对既有规划本身的两点优化（已落进任务书）：

1. **F0-J0 剥离 embedding**：路线文档 F0-J0 的验证清单本就不含向量（embedding/混合召回属 F0-J1）。任务书明确本轮禁用任何 embedding/外部模型调用，探针用 BM25/关键词即可验证全部机制项——探针更薄、更快、零外发风险。
2. **预授权降级分支**：RAGFlow arm64 构建失败（限时 2 次尝试）→ 自动转"OpenSearch 单路线 + RAGFlow 失败证据留档"，不需要中途请示。这符合准入门"自建/放弃必须留下成熟候选的失败证据"的规则，也防止弱模型死磕。

## 6. 交接给 dsv4 flash 的方式

1. **唯一任务来源**：`F0-J0_TASKBOOK.md`（同目录）。开场白直接给它："你是执行者，读 `F0-J0_TASKBOOK.md` 并按顺序执行；断线先读 `PROGRESS.md` 最后一节接续。"
2. **它需要的仅有三个文件**：任务书、PROGRESS.md（接续用）、BLOCKED.md（登记卡点用）。不要让它先读全部规划文档——任务书里已内嵌所需事实。
3. **你在每个任务完成后检查四件事**：
   - PROGRESS.md 有 ≤10 行回执且贴了真实命令输出；
   - 没有任何闸门被打开（acceptance.json 未变、SEARCH 仍 NOT_READY）；
   - 三个 F0-I 产物 SHA 未变；
   - 聊天输出里没有 DSN、源文件名、chunk 正文。
4. **停止条件**：同一验收连败 3 次换项、最多 8 轮、拿不准写 BLOCKED.md——任务书已内置，你只需在它报 BLOCKED 时裁决。
