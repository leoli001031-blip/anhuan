# 环保托管运营平台后续开发进程规划

> **HISTORICAL_SNAPSHOT / SUPERSEDED。** 本文保留 2026-08-07 的排期快照，不代表当前阶段；现役状态见 [PROJECT_STATUS.md](./PROJECT_STATUS.md)。

版本：v1.1
日期：2026-08-07
状态：`F0-J1_APPROVED / SEARCH_NOT_READY / NOT_PRODUCTION`

> 本文件是总体进程视图，不是可执行任务书。每个阶段的执行以对应 TASKBOOK.md 为准。

---

## 当前位置

| 阶段 | 状态 | 关键产物 |
|---|---|---|
| F0-A ~ F0-I | 已交付 | 26 份 Fixture / PostgreSQL RLS / pgcrypto 密文 / 300 canonical child chunks |
| F0-J0 | 已交付 | `artifacts/f0j0-retrieval-selection/v0.1/selection.json`：RAGFlow v0.26.4 + 豆包 `doubao-embedding-vision` 已验证可行 |
| F0-J1 | **已批准，待执行** | `F0-J1_TASKBOOK.md` 已定稿；交付**检索 + 证据化问答**（含 LLM） |
| F1 ~ F3 | 未开始 | 见下文 |

---

## 更新说明

按 2026-08-07 领导确认，**F0-J1 直接包含 LLM 证据化问答**，原 F0-K（证据化问答）合并进 F0-J1，不再作为独立阶段。后续进程简化为四阶段：F0-J1 → F1 → F2 → F3。

---

## 阶段总图

```
F0-J1 检索 + 证据化问答（含 LLM）
    │ 输出：RAGFlow + 豆包 embedding 检索，PG RLS 复核，LLM 生成带 citation 回答，拒答规则
    │ 边界：不做 UI、不对外发布、不替代专业判断、不宣称准确率
    ▼
F1 真实平台壳
    │ 输出：OIDC 登录、S3 对象存储、企业/厂区/伙伴权限、FastAPI + React、OpenTelemetry
    │ 前置：Q01/Q07/Q08 至少部分确认，真实客户授权
    ▼
F2 环保服务闭环
    │ 输出：企业档案、政策库、finding、整改任务、复核、日历、时间线、报告、门户、最小 CRM
    │ 前置：Q03/Q09 确认，真实业务流程样例
    ▼
F3 受控试点与生产准备
    │ 输出：真实样本验收、备份恢复、告警、退租、事故 Runbook、有限生产
    │ 前置：Q04/Q05-B/Q06/Q07/Q08/Q11/Q12 全部确认
```

---

## 各阶段目标与交付

### F0-J1：检索 + 证据化问答（当前）

- **目标**：在 RAGFlow + 豆包 embedding 上跑通"查询 → 召回 chunk ID → PG RLS 复核 → 授权正文 + 引用 → LLM 生成带 citation 回答"的完整链路。
- **关键交付**：
  - `src/platform_foundation/f0j1/` 生产级适配器（reader/ragflow_client/retrieval/citation/llm_client/qa_service）；
  - `tests/test_f0j1_retrieval_qa.py` 覆盖 C1~C12 与 QA 规则；
  - `artifacts/f0j1-retrieval-qa/v0.1/retrieval_qa.json` + `retrieval_qa.md`。
- **必须坚持的边界**：
  - 不做聊天 UI；
  - 不对外发布；
  - 不替代环保专业判断；
  - 不宣称准确率；
  - PostgreSQL 只读；
  - F0-I 三产物零漂移。
- **进入 F1 的前置**：
  - F0-J1 完成条件 8 条全部满足；
  - Q01（验收终点）、Q07（部署方式）、Q08（资产归属）有明确结论；
  - 至少一家真实客户/试点企业书面授权（Q02）。

### F1：真实平台壳

- **目标**：把现有 Fixture 工程升级为可对外提供服务的平台壳，但仍不开放真实业务。
- **关键交付**：
  - OIDC 身份服务（成熟方案）；
  - S3 兼容对象存储，替代 `/private/tmp` vault；
  - React 前端 + FastAPI 后端；
  - OpenTelemetry 可观测性；
  - 邀请登录、企业/厂区/管理员/伙伴权限矩阵；
  - 真实上传状态、任务恢复、审计后台、管理后台。
- **边界**：
  - 仍用 Fixture 数据或合成数据；
  - 不接入真实客户生产环境；
  - 法规库、finding、报告仍为候选/草稿态。
- **进入 F2 的前置**：
  - F1 内部 UAT 通过；
  - Q03（当前人工流程与成功重点）确认；
  - Q09（报告模板）提供样例。

### F2：环保服务闭环

- **目标**：实现环保托管业务的核心闭环。
- **关键交付**：
  - 企业档案、厂区、许可证、服务项目；
  - 共享政策库与资料分类；
  - finding 候选生成、人工确认、整改任务、复核关闭；
  - 合规日历、业务时间线；
  - 报告冻结、签发、发布；
  - 企业门户、最小 CRM。
- **边界**：
  - AI 只生成候选与草稿；
  - 正式结论和发布仍走权限与责任流程；
  - 专业责任人（Q06）明确。
- **进入 F3 的前置**：
  - F2 在 1 家试点企业完整跑通（含 finding→整改→复核→报告）；
  - Q04（真实文件与容量）确认并扩样；
  - Q05-B、Q06、Q07、Q08、Q11、Q12 全部确认。

### F3：受控试点与生产准备

- **目标**：在真实客户、真实资料、真实责任边界下做有限生产。
- **关键交付**：
  - 真实样本验收与 Acceptance Gold；
  - 备份恢复演练、PITR、故障域；
  - 告警、退租导出/删除、事故 Runbook；
  - 有限生产开闸（按客户授权范围）；
  - 正式 SLA 与支持流程。
- **边界**：
  - 没有人工 Gold 时仍标记 `ACCURACY_NOT_EVALUATED`；
  - 任何超出授权范围的能力保持 CLOSED。

---

## 当前必须决策/处理的事项

| ID | 事项 | 影响 | 建议处理时机 |
|---|---|---|---|
| R1 | 项目无 `.git` | 代码单点丢失风险 | **已处理：git init + 首提交完成** |
| R2 | pgcrypto key 在 `/private/tmp` | 重启/清理后密文不可解 | F0-J1 内加存在性检查；F1 前迁持久目录 |
| R3 | 13 个 `f0f_test_*` 孤儿 DB + 14 个 dbtest key | 占用空间、可能混淆 | 单独授权一次清理任务，不混入 F0-J1 |
| R4 | F0-J1 需要 Ark key | 无 key 无法启动 embedding/LLM | **开工前必须提供**（当前 `/private/tmp` 下未找到） |
| R5 | F0-J1 已含 LLM | 范围比原规划扩大 | 已落进任务书；需确认模型名（默认 `doubao-pro`） |

---

## 交给执行者的优先级

1. **立即**：确认 F0-J1 范围（检索 + LLM 证据化问答）和 LLM 模型名（默认 `doubao-pro`）。
2. **开工前**：提供 Ark API key，存放为 `/private/tmp/anhuan-f0j1-secrets/ark_api_key`（0600）。
3. **F0-J1 执行期间**：不要扩大范围到 UI/真实客户数据/专业结论发布。
4. **F0-J1 收口时**：根据完成条件 8 条裁决是否进入 F1 或暂停。

---

## 文件索引

- 执行：`F0-J1_TASKBOOK.md`
- 历史证据：`PROGRESS.md`、`BLOCKED.md`
- 选型记录：`artifacts/f0j0-retrieval-selection/v0.1/selection.json`
- 总体规划：`后续开发路线_成熟方案优先_2026-08-06.md`
- 待确认事项：`开工前决策与待确认事项_2026-08-04.md`
