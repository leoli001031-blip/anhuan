# 安环项目协作规则

本仓库是本地、非生产的安环运营平台原型。局部检查、随机 scratch 冒烟或前端构建都不等于发布验收。

## 现役权威

- 2026-09-08用户已明确完整一期正式上线并要求 `/goal` 持续完成开发、debug、测试、真实质量验收、部署和运维。新执行范围以 [PHASE1_GO_LIVE_PLAN.md](./PHASE1_GO_LIVE_PLAN.md) 为准，当前事实以 [PHASE1_GO_LIVE_PROGRESS.md](./PHASE1_GO_LIVE_PROGRESS.md) 为准；不得将首批缺陷修完等同完整目标完成。下述历史本地原型停点不阻止这次已授权的一期工作；所需真实材料、业务验收和具体部署参数仍按实际输入处理。

- 开工先读 [PROJECT_STATUS.md](./PROJECT_STATUS.md)，再读当前阶段的 `TASKBOOK / PROGRESS / BLOCKED`。
- A-Eco 分析报告候选的现役入口依次为 `RELEASE_CANDIDATE_PROGRESS.md` 末尾事实矩阵、`RELEASE_CANDIDATE_BLOCKED.md`、`RELEASE_CANDIDATE_REVIEW.md`；前文阶段快照和根历史流水账不得覆盖它们。
- 根 `PROGRESS.md`、`BLOCKED.md` 是历史流水账，不是现役状态来源。
- F1.1.1 保持 `F1_1_1_PAUSED_NOT_ACCEPTED`；不得自动恢复 formal、reverse、SBOM、clean rebuild 或 M4。
- P2-P8 的精确验证层级以 `PROJECT_STATUS.md` 为准；不得自动进入真实 UAT、生产或正式小程序发布。

## 修改边界

- 修改前检查 `git status --short --branch` 和 `git worktree list`，保留其他 worktree 的用户改动。
- Alembic 只允许一条线性源码 head，当前为 `f1_0044`；`migrate_f1` 目标闭集为 `f1_0014..f1_0044`。默认工程/verify/seed/backup 仍锁 `f1_0014 / 35`，material-RAG 专属目标为 `f1_0016`，analysis-report 专属 migrator 请求 `f1_0044 / 55`。不得把专属目标写回默认栈。部署须针对实际数据库版本完成前向升级与恢复演练；历史精确 `f1_0017` 备份方案只适用于对应旧环境，其他 head 不能冒用其证明。失败走已演练的恢复式回滚，不做 downgrade。`f1_0027` 在报告/版本真实语义变化后写入私有 transition，管理、审核、业务审计和健康事件必须绑定同事务、同动作、同actor及from/to；时间戳或仅更新xmin不是转换证明。归档/恢复沿用报告→版本行锁顺序，归档期间业务动作不得混写；withdraw仍遵循现役合同。`f1_0028` 保留旧解析事实，新增提取契约与OCR后端身份；旧ready只能追加合规分析revision，旧confirmed必须人工复核，索引重建原子替换且不得改冻结报告。`f1_0029` 接受整页渲染OCR与提取契约3，保留旧行身份，不得重标旧证据。`f1_0030` 增加组织类型并只做明确关系回填与session-v2身份投影，不代表完整四角色专业权限已收口。`f1_0031` 通过独立受限definer完成客户门户开通/暂停/恢复及负责人邀请，开通不建立服务商在客户租户的membership；暂停使未消费邀请永久撤销，恢复必须显式操作。`f1_0032` 成员停用保留历史行；普通RLS排除已停用成员，管理与历史报告收尾采用限定例外。管理员变更按企业串行并保护最后一名有效管理员；报告生成终态及门户/邀请命令与停用通过成员行锁确定提交顺序。`f1_0033` 为新客户创建保存不可变请求ID与原始参数，重放先核验当前服务商管理员权限并持成员锁到提交；旧客户数据不回填伪造请求。`f1_0034` 新增独立原生证据任务/不可变修订/加密片段三表；DOCX释放与任务同事务登记，源行→job→当前成员行锁及实时租约共同限制结果提交；partial保存完整缺口且零可用片段。v2片段身份不能伪装PDF页或覆盖旧v1；新私有pgcrypto依赖须随当前候选恢复演练核验。`f1_0035` 扩展XLSX/JPEG释放与持久化任务、严格格式定位、图片处理身份，统一Python与PostgreSQL的Unicode ASCII canonical编码；旧DOCX修订/AAD保持，部分提取仍零片段。`f1_0036` 新增独立人工修订与加密片段，确认/撤销均追加，不覆盖原提取；来源锁与当前成员锁约束提交，请求身份重放与预期revision避免重复/覆盖；PDF继续使用旧v1提取身份，新人工片段使用独立AAD。`f1_0037` 统一当前有效证据与格式定位引用；当前确认覆盖原提取、撤销不回退；API受限读取两域，后台仍无直接原生表读权。报告保存位置、revision、片段和正文SHA，旧PDF引用不重标；完整语料超预算明确失败。`f1_0038` 增加当前管理员显式恢复原生任务及不可变请求回执；历史缺任务只补一次，blocked才重绑/重置，running/done不覆盖；恢复与报告续接同事务。`f1_0039` 为当前服务商管理员提供复核原件入口；必须匹配当前原提取revision与片段，人工确认/撤销不妨碍原件核查，客户QA权限不扩大。`f1_0040` 增加独立源读取登录与只读definer；后台用当前任务及租约领取源字节，不能指定对象key，读取持来源与任务锁并核验hash和有效期。`f1_0041` 新增独立报告生成登录，仅能用投递/租约调用领取、证据读取与结果收尾；租户/客户/actor由DB确定，运行卷不再持API密码；无表读写/发布/成员管理权。`f1_0042` 拆分摄取数据库角色，按当前投递/原件/成员限定读写及持久交接；语句和提交前核验租约，禁止旧分析追加内容、源身份或发布写入，运行卷不持API密码。`f1_0043` 按摄取投递和处理租约限定原件读取/预览写入，源网关持来源与成员锁；预览按内容hash寻址防止迟到覆盖，旧manifest只在新地址缺失时兼容读取。摄取卷无MinIO凭据，网关只能读源/读写预览且无删除或列对象权限。`f1_0044` 新增加密OCR结果缓存，仅经当前源任务租约函数读写，完整输入身份相同才复用；成功分析不清缓存，旧checkpoint不迁入或重标。
- 复用现有租户上下文和 RLS；跨租户详情返回 404、集合零行；关键状态变化与 timeline/audit 同事务。
- 不修改历史 F0/F1 证据、REJECTED 批次或冻结原件，不清理共享数据库、对象、卷、容器或 secret 目录。
- 未经明确授权不 commit、push、deploy、删除文件或写生产。

## 目录与验证

- 后端：`src/platform_foundation/f1/`；迁移：`infra/f1/alembic/versions/`；前端：`src/web/`；直接检查：`tests/`。
- 前端开发命令位于 `src/web/package.json`；共享栈配置为 `infra/f1/docker-compose.yml`，不得自行启停。
- 默认遵循全局轻量原型预算；只报告实际达到的 `NOT_TESTED / SMOKE_PASSED / TARGETED_TEST_PASSED / RELEASE_VERIFIED`。
- `statusCheckRollup=[]` 只能写“无 CI checks”，不得写“CI 通过”；本地组件门、人工验收、PR 合并、远端部署与生产必须分开记状态。
- 没有新授权时停在本地原型，不续做发布验收、部署或下一产品阶段。
