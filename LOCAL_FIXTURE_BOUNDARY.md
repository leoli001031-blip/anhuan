# 本地 Fixture 使用边界

旧环境资料只作为 `LOCAL_FIXTURE / UNLABELED` 的本机隔离测试输入。原件、文件名和逐文件登记表不进入当前 clean checkout；历史文档中的文件/页数统计是各自日期的快照，不应拼成新的现役清单。

固定边界：

- 不把 Fixture 当成真实客户事实、Seed Gold、Acceptance Gold 或准确率证据。
- 不自动授权外部模型处理、公开展示、再分发、客户 UAT 或生产使用。
- 代码与产物不得复制原件正文、路径、文件名、PII、token、DSN 或对象 URL。
- 当前 P2-P7 验证使用合成身份、登记 Fixture 或随机 scratch；其结果只证明对应工程链路。
- 需要重新核验原件清单、SHA 或授权范围时，必须在明确的只读资料边界内单独执行，不能从本文件推断。

本页仅替代旧文档指向未跟踪本机目录的断链，不恢复或公开该目录内容。
