# F1.1.1 Repair Blocked

- `F1_1_1_PAUSED_NOT_ACCEPTED`（现役）：用户已把发布验收暂停在修复阶段，后续 P2-P8 本地原型开发不解除该结论。formal/reverse/SBOM/clean/M4 不自动恢复；tracked v0.3 仍为 `F1_1_1_REJECTED`。以下条目是保留的历史阻塞与授权记录。

- `CREDENTIAL_MATERIAL_IN_LOCAL_HISTORY`：既有三个Claude本地提交含开发期固定材料。禁止复述、复用、改写历史；本轮只保证新增工作树、日志和产物泄漏为0。此项不阻塞Fixture修复，但最终不得宣称Git历史无敏感材料。
- `DEFINER_ROLE_AUTHORIZATION_RESOLVED_FOR_SCRATCH`：用户已授权Codex接手并继续本任务书范围开发；采用8个NOLOGIN/NOBYPASSRLS/零membership最小函数owner域属于既定租户安全修复。仅允许写入迁移代码并应用到随机scratch做owner/ACL/跨租户攻击矩阵；共享数据库仍保持只读，M4前不应用。
- `FROZEN_F0_LIVE_ISOLATION_AUTHORIZED_PENDING_IMPLEMENTATION`：用户于2026-08-09明确回复“选1”，授权为冻结F0 live测试及最小运行配置增加仅formal使用的显式随机资源参数。默认行为必须保持不变；26原件、历史数据库、旧迁移、共享Docker/RAGFlow/provider仍只读。实现与独立回归完成前，formal继续固定fail-closed且不能产READY；禁止过滤模块、新增skip、monkeypatch或事后清共享资源假绿。
