# 环保托管运营平台后续开发路线：成熟方案优先

> **HISTORICAL_SNAPSHOT / SUPERSEDED。** 本文保留 2026-08-06 的路线决策，不代表当前阶段；现役状态见 [PROJECT_STATUS.md](./PROJECT_STATUS.md)。

版本：v0.1 规划稿  
日期：2026-08-06  
状态：`PLANNING_DRAFT / F0-I_IN_PROGRESS / NOT_PRODUCTION`

> 目标：保留已经建立的证据、安全和环保业务边界；文档解析、OCR、RAG、身份、存储、观测等通用能力优先采用成熟方案，通过薄适配层接入。只有成熟方案无法满足明确验收条件时才自建。

## 1. 当前执行快照

- F0-A～F0-H 已形成登记资料、页级路由、PostgreSQL/RLS、受控正文、PP-OCRv6 本地运行等 Fixture 底座；F0-H 最终状态仍为 `ACCURACY_NOT_EVALUATED / SEARCH_NOT_READY / NOT_PRODUCTION`。
- F0-I 正在把26份登记资料转成加密、可重组、可追溯的 canonical blocks 与 parent/child chunks。
- 当前 Alembic head 为 `f0d_0006`；`tests/test_f0i_canonical_chunks.py` 有116项独立测试，`PROGRESS.md` 已登记116项通过。
- `persistence.py`、`replay.py`、`artifacts.py`、`f0i_reverse_verify.py` 和 CLI 已存在；但 `artifacts/f0i-canonical-chunks/v0.1` 尚未生成，进度记录也尚无真实 smoke→full→full、reverse、全仓最终回归证据。
- 因此当前结论是：`F0-I_IMPLEMENTATION_IN_PROGRESS`，不是 F0-I 已交付；RAG、embedding、检索、引用和问答均尚未开始。

## 2. 自建与采用的固定边界

| 必须掌握在自己系统中 | 优先采用成熟方案 |
|---|---|
| 企业/厂区/服务周期与伙伴权限 | PDF、Office、图片解析器 |
| PostgreSQL事实源、RLS和复合约束 | OCR与版面/表格模型 |
| 原件版本、SHA、证据定位和审计 | RAG编排、embedding、混合召回、重排 |
| finding、整改、日历、时间线、报告状态 | OIDC身份服务、S3对象存储 |
| 专业确认和发布责任边界 | 可观测性、错误跟踪、PDF渲染 |

成熟组件只能成为可替换执行器，不能成为权限或业务事实源。所有外部索引返回候选ID后，必须回 PostgreSQL 重新做企业、版本、密级和状态鉴权。

## 3. 成熟方案准入门

任何新的通用模块开工前必须先完成一页选型记录：至少列2个仍维护的候选，写明官方来源、许可证、稳定版本、部署资源、arm64支持、离线/数据外发、升级迁移、可观测性和退出方案。满足以下条件才接入：

1. 能保留我们的稳定 document/version/page/chunk ID 和 provenance；
2. 可重复导入、删除、重建，失败不会改变 PostgreSQL 事实；
3. 租户过滤可验证，但不被当成最终授权；
4. 可固定版本并回滚，许可证允许目标交付方式；
5. 资源和运维复杂度适合一期。

若决定自建，必须在 `BLOCKED.md` 写清所有成熟候选分别在哪条硬条件失败；“接起来麻烦”不是自建理由。

## 4. 后续实施顺序

### A. 先完成 F0-I，不中途换架构

完成真实 smoke→full→full、第二次 full 零新增/零OCR、reverse、三项聚合产物和全仓回归；继续保持搜索、外部LLM和生产关闭。F0-I 的 canonical chunks 是后续所有检索方案的唯一输入合同。

### B. F0-J0：成熟 RAG 方案选型与可撤销探针

只做选型和薄适配，不做聊天产品。首选候选为 RAGFlow，保底对照为直接 OpenSearch；两路读取完全相同的 canonical child chunks，不重新OCR或重新解释原件。验证：稳定ID/metadata、增量导入、幂等、删除同步、索引清空重建、父子块、metadata filter、引用回传、进程重启、arm64部署、资源占用和零跨租户候选。

RAGFlow 未通过前不得把它写成既定架构；通过后也只定位为 `Retrieval Sidecar`。若其额外 MySQL/MinIO/Redis/Elasticsearch 运维负担高于收益，选择直接 OpenSearch，不再自建搜索引擎。

### C. F0-J1：只读检索与引用

由入选成熟引擎负责 embedding、BM25/向量混合召回及可选重排；API只接受业务语料域，不接受客户端索引名。返回候选chunk ID后由 PostgreSQL RLS复核，再读取授权正文并生成页码/bbox引用。先交付搜索与拒答，不接LLM。

### D. F0-K：证据化问答

在检索链稳定后接入可替换 LLM provider；回答中的每个事实必须绑定授权citation，无证据则拒答。Fixture阶段只验证链路和泄漏，不宣称问答准确率。双OCR或多模型一致只能作为 Silver/分流信号，不能冒充 Gold。

### E. F1：真实平台壳

采用成熟 OIDC、S3兼容对象存储、React/FastAPI、OpenTelemetry和固定Chromium；补齐邀请登录、企业/厂区/伙伴权限、真实上传状态、任务恢复、审计和管理后台。优先托管服务，一期不上微服务/Kubernetes。

### F. F2：环保服务闭环

实现企业档案、共享政策库、资料分类、finding候选、整改任务、复核、日历、业务时间线、报告冻结、企业门户和最小CRM。AI只生成候选与草稿，正式结论和发布仍走权限与责任流程。

### G. F3：受控试点与生产准备

真实客户、地区行业、资料授权、外部处理、专业责任和UAT未确认前保持关闭。确认后再做真实样本验收、备份恢复、告警、退租、事故Runbook和有限生产；没有人工Gold时只能标记 `ACCURACY_NOT_EVALUATED`。

## 5. 当前候选池（2026-08-06快照，不等于选定）

- [RAGFlow v0.26.4](https://github.com/infiniflow/ragflow/releases/tag/v0.26.4)，Apache-2.0：RAG编排、父子块、混合检索和调试工作台候选；官方自托管要求至少16GB内存/50GB磁盘，并带多项依赖，必须先做目标机器探针。
- [OpenSearch 3.8.0](https://github.com/opensearch-project/OpenSearch/releases/tag/3.8.0)，Apache-2.0：直接检索后端与RAGFlow保底对照。
- [Docling v2.118.0](https://github.com/docling-project/docling/releases/tag/v2.118.0)，MIT：未来复杂PDF、DOCX、XLSX、表格/版面解析候选；不得未经对照替换已冻结证据。
- [PaddleOCR v3.7.0](https://github.com/PaddlePaddle/PaddleOCR/releases/tag/v3.7.0)，Apache-2.0：PP-OCRv6与PP-StructureV3正式适配候选；当前F0-H继续冻结可回滚。

## 6. 完成定义

1. 每一阶段都能明确回答“事实源在哪里、成熟组件负责什么、如何替换、坏了如何恢复”，不存在第二套不可重建的业务事实。
2. 任何“可用、准确、UAT、生产”状态都必须有对应验收证据；Fixture、双模型一致或测试通过不得升级成准确率与专业结论。
